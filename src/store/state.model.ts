export class State {
    counter: number;
    showCounter: boolean;

    constructor(counter: number, showCounter: boolean) {
        this.counter = counter;
        this.showCounter = showCounter;
    }
}